const leapYears = function() {
const year = Math.pow(10, 1000);

    if (year / 4 == parseInt() && year / 400 == parseInt()) {
        return true;
    } else  {
        (year / 100)
        return false;
    }
}

leapYears (700);

// Do not edit below this line
module.exports = leapYears;